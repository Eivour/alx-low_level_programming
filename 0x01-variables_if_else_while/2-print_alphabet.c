#include <stdio.h>
/**
 * main - This is the entry point for the main function
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char alphabet - 'a';
	while (alphabet <= 'z')
	{
		putchar(alphabet);
		alphabet++;
	}

	putchar('\n');
	return (0);
}
